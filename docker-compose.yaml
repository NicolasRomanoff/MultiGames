services:
  server:
    build:
      dockerfile: ./apps/server/dockerfile
      context: .
    ports:
      - 3000
    healthcheck:
      test: ["CMD", "curl", "http://localhost:3000/api/"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s
